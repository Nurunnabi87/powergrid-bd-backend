# The project folder name contains "&", which cmd.exe treats as a command
# separator and which breaks every npm script / npx shim on Windows.
# Running scripts through bash (Git Bash locally, /bin/bash on Vercel)
# avoids the splitting entirely.
script-shell=bash
